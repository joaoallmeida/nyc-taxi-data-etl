
# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'dbt_transformation'
version: '1.0.0'
config-version: 2

# This setting configures which "profile" dbt uses for this project.
profile: 'dbt_transformation'

# These configurations specify where dbt should look for different types of files.
# The `model-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

# In this example config, we tell dbt to build all models in the example/
# directory as views. These settings can be overridden in the individual model
# files using the `{{ config(...) }}` macro.

models:
  dbt_transformation:
    silver:
      fat_green_trips:
        +materialized: table
        +on_table_exists: drop
        +schema: silver
        +post-hook:
          - "{{ export_to_minio('silver','fat_green_trips') }}"
      fat_yellow_trips:
        +materialized: table
        +schema: silver
        +on_table_exists: drop
        +post-hook:
          - "{{ export_to_minio('silver','fat_yellow_trips') }}"
      fat_fhv_trips:
        +materialized: table
        +schema: silver
        +on_table_exists: drop
        +post-hook:
          - "{{ export_to_minio('silver','fat_fhv_trips') }}"
      dim_payments:
        +materialized: table
        +schema: silver
        +on_table_exists: drop
        +post-hook:
          - "{{ export_to_minio('silver','dim_payments') }}"
      dim_rates:
        +materialized: table
        +schema: silver
        +on_table_exists: drop
        +post-hook:
          - "{{ export_to_minio('silver','dim_rates') }}"
      dim_trips:
        +materialized: table
        +schema: silver
        +on_table_exists: drop
        +post-hook:
          - "{{ export_to_minio('silver','dim_trips') }}"
      dim_vendors:
        +materialized: table
        +schema: silver
        +on_table_exists: drop
        +post-hook:
          - "{{ export_to_minio('silver','dim_vendors') }}"
    gold:
      green_trips_amount_by_payments:
        +materialized: table
        +on_table_exists: drop
        +schema: gold
        +post-hook:
          - "{{ export_to_minio('gold','green_trips_amount_by_payments') }}"
